# Checkpoint — Frontend Sprint-02 (2026-05-27)

## Agente
Frontend (Angular + Angular Material)

## Branch
`feature/s02-transactions-frontend`

## Status
BUILD SUCCESS — zero erros de compilacao, 2 warnings de budget (nao criticos)

## O que foi entregue

### Modelos TypeScript (`core/models/`)
- `category.models.ts` — Category, CategoryType, CreateCategoryRequest
- `person.models.ts` — Person
- `transaction.models.ts` — Transaction, TransactionType, PaymentMethod, SplitRule,
  CategorySummary, PersonSummary, CreateTransactionRequest, UpdateTransactionRequest

### Servicos HTTP (`core/services/`)
- `category.service.ts` — GET /api/categories?type=...
- `person.service.ts` — GET /api/persons
- `transaction.service.ts` — GET/POST/PUT/DELETE /api/transactions

### Modulo transactions (`transactions/`)
- `transactions.routes.ts` — rota lazy para TransactionListComponent
- `transaction-list/` — lista com navegacao de mes, resumo de receitas/despesas,
  estados de loading e vazio, acoes de editar/deletar por item, FAB para novo lancamento
- `transaction-form/` — formulario reativo com toggle Despesa/Receita, todos os campos
  do contrato, carregamento dinamico de categorias e pessoas, exibicao de erros da API

### Integracoes
- `app.routes.ts` — rota lazy `/transactions` com authGuard
- `dashboard.component.ts` — atualizado com navegacao para lancamentos e atalhos
  de features futuras (cartoes, dashboard, acerto de contas)

## Commits na branch
1. `adiciona modelos TypeScript: Transaction, Category e Person`
2. `adiciona servicos HTTP: CategoryService, PersonService e TransactionService`
3. `adiciona TransactionListComponent e TransactionFormComponent com roteamento lazy`
4. `atualiza dashboard: navegacao para lancamentos e atalhos de features futuras`

## Pendencias / proximos passos
- Build warnings de budget: considerar dividir transaction-list.scss ou aumentar budget
  no angular.json se necessario
- Parcelamento (installmentsTotal > 1) reservado para Sprint 03
- Nomes de pessoas dinamicos (atualmente "So A" / "So B") serao melhorados quando
  PersonService retornar nomes reais do banco
- Modo escuro: funcional via CSS variables herdadas do styles.scss

## Notas tecnicas
- Campo `installmentsTotal` enviado fixo como `1` (parcelamento Sprint 03)
- `version` do optimistic locking passado no UpdateTransactionRequest
- Categorias filtradas por tipo ao trocar entre Despesa/Receita no formulario
- Confirmacao de exclusao via snackbar com acao "Excluir" (5 segundos)
