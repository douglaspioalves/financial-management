# Checkpoint Backend — 2026-05-30 02:11

## Feito nesta sessão

### Tarefa S-07-01a: GET /api/export?month=yyyy-MM&format=csv|xlsx

**Branch:** `feature/s07-backend-export`

**Arquivos criados/modificados:**
- `backend/pom.xml` — adicionada dependência `poi-ooxml 5.3.0`
- `backend/src/main/java/com/gastos/service/ExportService.java` — novo service com:
  - `exportCsv(YearMonth)` → `byte[]` (UTF-8 com BOM, separador vírgula)
  - `exportXlsx(YearMonth)` → `byte[]` (Apache POI XSSFWorkbook, cabeçalho negrito)
  - `record ExportRow` com 8 campos em pt-br
  - Consolida: cashExpenses + incomes (por transaction.date) + installments (por referenceMonth)
  - Ordenação: data ASC, descrição ASC
- `backend/src/main/java/com/gastos/controller/ExportController.java` — novo controller:
  - `GET /api/export?month=yyyy-MM&format=csv|xlsx`
  - month opcional (usa mês corrente se ausente)
  - format obrigatório (400 se ausente/inválido)
  - Requer JWT (sem exceção no SecurityConfig)

**Commits:**
1. `e4797fa` — feat(export): adiciona dependência Apache POI + ExportService (csv e xlsx)
2. `2997ba5` — feat(export): ExportController — GET /api/export?month&format

**mvn compile:** BUILD SUCCESS (exit code 0)

## Pendente

- PR para master (aguarda revisão do agente revisor)
- Testes unitários para ExportService (agente QA)

## Próximo passo imediato

Abrir PR `feature/s07-backend-export` → `master` para revisão.
