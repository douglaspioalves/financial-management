# Daily — 2026-05-27
**Sprint:** 01 | **Dia do sprint:** 2/10
**Agentes ativos:** DBA (x2 paralelo), Backend (x3 paralelo+sequencial), Frontend (x3 paralelo+sequencial), DevOps (CI noturno)

---

## MANHÃ — Planejamento do dia

### DBA
- Executar S-01-02: V2__initial_schema.sql + V3__seeds.sql em paralelo

### Backend
- Executar S-01-03: domínio + JWT infra em paralelo; AuthService+Controller sequencial

### Frontend
- Executar S-01-04: design system + auth logic em paralelo; telas sequencial

---

## FIM DO DIA — Fechamento

### DBA
- **O que foi feito:**
  - `V2__initial_schema.sql` — 8 tabelas (users, person, category, card, transaction, installment, budget, recurring_rule), todas as FKs, CHECK constraints (enums, valores positivos, dias 1–31, card_id só com CREDIT, reference_month e month sempre dia 1), version para optimistic locking, 7 índices incluindo parcial para recurring_rule ativo
  - `V3__seeds.sql` — 2 persons (UUIDs fixos ...0001/0002, nomes neutros, cores do design system), 12 categorias (UUIDs fixos ...0101 a ...0112, 8 EXPENSE + 2 INCOME + 2 BOTH), ON CONFLICT DO NOTHING (idempotente)
  - Decisão: tabela `users` (não `user` — palavra reservada no PostgreSQL)
- **Pendente:** validação do `docker compose up` num banco zerado (última tarefa S-01-02)

### Backend
- **O que foi feito:**
  - `User.java` — entity JPA `@Table("users")`, implementa UserDetails (getUsername → email)
  - `UserRepository` — findByEmail, existsByEmail
  - DTOs: `RegisterRequest`, `LoginRequest`, `AuthResponse` (records com Bean Validation pt-br)
  - `UserDetailsServiceImpl` — carrega usuário por email
  - `JwtService` — JJWT 0.12.6, generateToken/extractUsername/isTokenValid
  - `JwtAuthenticationFilter` — OncePerRequestFilter, Bearer token → SecurityContext
  - `AuthService` — register (BCrypt) + login (AuthenticationManager)
  - `AuthController` — POST /api/auth/register (201) + POST /api/auth/login (200)
  - `SecurityConfig` — reescrito: DaoAuthenticationProvider, BCryptPasswordEncoder, filtro JWT
  - `GlobalExceptionHandler` — validação (400 por campo) + IllegalArgument (409)
  - `./mvnw test` — BUILD SUCCESS
- **Pendente:** testes de integração QA (S-01-03 parcialmente aberta)

### Frontend
- **O que foi feito:**
  - `styles.scss` — design system completo: CSS variables, tema Angular Material MD3 (paleta azure), modo escuro via `.dark-theme`
  - `ThemeService` — signal isDark, toggle(), persistência em localStorage, detecção de preferência do sistema
  - `AuthService` — login/register/logout, currentUser (signal), isAuthenticated (computed), token em localStorage
  - `authGuard` + `guestGuard` — guards funcionais Angular 21
  - `authInterceptor` — injeta Bearer token em todas as requisições
  - `LoginComponent` + `RegisterComponent` — reactive forms, Material outline fields, validações pt-br, spinner, exibição de erro da API, link entre telas
  - `DashboardComponent` — placeholder com saudação ao usuário e botão logout
  - App shell atualizado: router-outlet + botão flutuante de toggle de tema (fixed top-right)
  - `npm run build` — BUILD SUCCESS, lazy chunks auth-routes + dashboard-component gerados
- **Pendente:** nenhum (S-01-04 completa)

### DevOps (execução noturna)
- **O que foi feito:**
  - `.github/workflows/ci.yml` — pipeline CI com jobs para backend (Maven, Java 21) e frontend (Node, Angular build)
- **Pendente:** validação do `docker compose up --build` com banco zerado

---

## Estado dos branches ao final do dia

| Branch | Estado | Conteúdo |
|---|---|---|
| `feature/esqueleto-monorepo` | Aberta | S-01-01 completa |
| `feature/migrations-iniciais` | Aberta | S-01-02 completa |
| `feature/auth-jwt` | Aberta | S-01-03 backend completo |
| `feature/git-actions` | **Ativa** | CI + S-01-04 frontend completo |
| `feature/tela-auth-frontend` | Abandonada | Substituída pela feature/git-actions |
| `main` | Desatualizada | Só o commit inicial |

**Próximo passo:** merge de todas as branches para main (ou PR sequencial), rodar QA, validar docker compose up.

---

## Impedimentos do dia

| Impedimento | Agente | Impacto | Ação |
|---|---|---|---|
| Limite de créditos à noite | Orquestrador | S-01-04 Agent 3 não rodou na hora | Retomado e concluído na manhã seguinte |
| Branch incorreta criada (feature/git-actions) | DevOps noturno | Contexto de branch errado | Aceito — branch tem histórico completo e coerente |
