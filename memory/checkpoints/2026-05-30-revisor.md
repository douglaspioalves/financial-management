# Checkpoint Revisor — 2026-05-30 14:00

## Revisado nesta sessão

### feature/s06-schema
- V9__sprint06_settlement_indexes.sql — OK
  - Índice composto (type, date) adicionado com justificativa técnica sólida
  - Índices existentes mantidos, sem DROP de nada
  - Schema `recurring_rule` confirmado já presente desde V2 (auditoria documentada)

### feature/s06-backend-recurring
- RecurringFrequency.java — OK
- RecurringRule.java — OK: @Version, BigDecimal, LocalDate, @PrePersist
- RecurringRuleRepository.java — OK
- RecurringRuleRequest.java — ATENÇÃO: campo `description` não tem @NotBlank; pode ser null/vazio e quebra a idempotência (existsByDescription com null = match em qualquer null)
- RecurringRuleResponse.java — OK (sem campos sensíveis)
- RecurringRuleService.java — OK: soft delete, @Scheduled cron 6h, idempotência implementada, erro por regra não para job todo
- RecurringRuleController.java — OK: @Valid, endpoints corretos

### feature/s06-backend-settlement
- SettlementService.java — OK: BigDecimal HALF_UP, new ArrayList() + sort alfabético, PROPORTIONAL sem receita → pendente, parcelas via Installment.referenceMonth
- SettlementController.java — OK: GET /api/settlement, month opcional (default = now), sem @Valid necessário (parâmetro scalar)
- PersonSettlementDTO.java — OK (sem campos sensíveis)
- SettlementResponse.java — OK (sem campos sensíveis)
- TransactionRepository.java — JPQL limpo, sem SQL injection, findIndividualIncomesByMonth filtra PERSON_A/PERSON_B corretamente

### feature/s06-tests
- SettlementServiceTest.java — 10 @Test, todos os cenários críticos cobertos (TC-S01 a TC-S10)
- SettlementIntegrationTest.java — 1 teste ativo (401 sem token), 5 @Disabled aguardando merge; testes de integração incompletos mas justificados

### feature/s06-frontend-recurring
- recurring-rule.service.ts — OK
- recurring-form.component.ts — OK: reutiliza CategoryService e PersonService existentes
- recurring-list.component.ts — OK
- recurring.routes.ts — OK: loadComponent (lazy)
- app.routes.ts — OK: canActivate[authGuard], loadChildren (lazy)
- models/recurring-rule.models.ts — OK

### feature/s06-frontend-settlement
- settlement.component.ts — OK: usa debtor/creditor do servidor, pendingProportional impede card de débito
- settlement.component.html — OK: lógica de exibição correta
- settlement.component.scss — OK: usa CSS variables do design system
- settlement.model.ts — OK
- settlement.service.ts — OK
- settlement.routes.ts — OK: loadComponent (lazy)
- app.routes.ts — OK: canActivate[authGuard], loadChildren (lazy)

### Segurança
- SecurityConfig: anyRequest().authenticated() → todos endpoints protegidos exceto /api/auth/**
- BCrypt confirmado
- Sem secrets hardcoded
- DTOs não expõem password_hash

## Pendente
- Nenhum arquivo pendente de revisão

## Veredito parcial
EM REVISÃO — 1 item de atenção em feature/s06-backend-recurring (description sem @NotBlank)
Não há bloqueante de segurança. Aguardando escrita do relatório final.
