# Checkpoint DevOps — 2026-05-30 13:00

## Feito nesta sessão

### Sessão anterior (12:36)
- Merge `feature/s07-backend-export` → master: ExportService.java, ExportController.java, Apache POI
- Merge `feature/s07-frontend-ux` → master: ExportService Angular, loading states, empty states, responsividade mobile
- Merge `feature/s07-tests` → master: FlowIntegrationTest.java (TC-F01 a TC-F08)
- `./mvnw test` → BUILD SUCCESS (109 testes, sessão anterior)
- `npm run build --configuration=production` → sem erros (7.5s)
- `git push origin master` → bem-sucedido

### Sessão atual (13:00)
- `git pull --rebase origin master` → já em sincronia (commit 4401520 do revisor de segurança)
- `./mvnw clean test` → BUILD SUCCESS — **129 testes, 0 falhas, 0 erros, 0 skipped**
- Fix `fix/enable-disabled-integration-tests` → master (merge --no-ff):
  - Removido @Disabled obsoleto de BudgetIntegrationTest e DashboardIntegrationTest
    (controllers mergeados no sprint 05, anotação esquecida)
  - Corrigidos TC-D4 e TC-D5 (DashboardIntegrationTest) para usar meses futuros isolados
    evitando acúmulo de dados no banco de teste compartilhado
- `git push origin master` → bem-sucedido (6bcbcb2)
- `npm run build --configuration=production` → BUILD SUCCESS (sem erros)
- `docker compose up --build` → **NÃO EXECUTADO** (Docker daemon indisponível no sandbox)

## Pendente

- `docker compose up --build` com banco zerado (requer Docker Desktop em máquina local)
- Modo escuro revisado em todas as telas (Frontend)
- Tratar erros de API com toasts/snackbar em pt-br (Frontend)
- Atualizar README com estado final (DevOps)
- Completar docs/api.md com todos os endpoints (Arquiteto)
- Tag `v1.0.0` no git (após documentação final e docker compose validado)
- Review registrada em `memory/reviews/review-sprint-07.md`
- Retrospectiva final em `memory/retros/retro-sprint-07.md`
- Learnings finais em `memory/learnings/`

## Próximo passo imediato

Rodar `docker compose up --build` em ambiente com Docker Desktop para validação final.
Depois: README final + docs/api.md → tag v1.0.0.

## Estado da infra

- `docker-compose.yml`: postgres:16-alpine + backend (Spring Boot 21 JRE alpine) + frontend (nginx:alpine)
- Healthcheck no postgres, depends_on com condition:service_healthy no backend
- Portas: 5432 (postgres), 8080 (backend), 4200 (frontend)
- Variáveis via .env (defaults seguros para dev: DB_USER=gastos, DB_PASSWORD=gastos_dev_password)
- JWT_SECRET com valor padrão base64 (NÃO usar em produção)
- Flyway migrations: V1 a V10+ cobrindo schema completo

## Métricas finais de testes (master em 6bcbcb2)

- Backend: **129 testes | 0 falhas | 0 erros | BUILD SUCCESS**
- Frontend: **npm run build production → sem erros**
