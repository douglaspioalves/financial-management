# Checkpoint Revisor — 2026-05-30 (Sprint 07)

## Revisado nesta sessão

### feature/s07-backend-export
- `ExportController.java` — OK. Sem endpoint público; validação de `format`; `Content-Disposition` usa `YearMonth.toString()` não interpolação bruta da URL; nenhum segredo hardcoded. Achado recomendado: a convenção do projeto proíbe `doubleValue()` para valores monetários — há um uso no XLSX (linha 105 do ExportService).
- `ExportService.java` — OK geral. Queries corretas por mês; BigDecimal em CSV (`toPlainString()`); labels em pt-br; 8 colunas cobertas. Achado recomendado: `doubleValue()` na célula XLSX (conversão de serialização, não aritmética — risco baixo mas viola convenção). Achado opcional: `splitRuleLabel` usa nomes genéricos "Pessoa A/B" em vez dos nomes reais dos participantes.
- `pom.xml` — poi-ooxml 5.3.0 adicionada corretamente; sem versão vulnerável conhecida.
- `SecurityConfig.java` (master, aplicado à branch) — regra `anyRequest().authenticated()` cobre `/api/export`; nenhuma exceção introduzida no sprint.

### feature/s07-frontend-ux
- `export.service.ts` — OK. Token JWT injetado via `authInterceptor` global (confirmado). Sem auth headers diretos no serviço (padrão correto via interceptor). `triggerDownload` gera nome de arquivo a partir da string `month` recebida do componente, não da URL.
- `transaction-list.component.ts/html/scss` — OK. Loading state, empty state, mensagens de erro em pt-br, design system via CSS variables, responsivo. Achado recomendado: `totalIncome`/`totalExpense` calculados com aritmética JS nativa (`number`) em vez de acumular BigDecimal — display-only, risco de exibição incorreta para valores muito grandes.
- `settlement.component.html` — OK UX. Skeleton de carregamento implementado.
- `auth.interceptor.ts` — OK. Injeta `Authorization: Bearer` em todas as requisições.

### feature/s07-tests
- `FlowIntegrationTest.java` — 8 testes ponta a ponta (TC-F01 a TC-F08) cobrindo registro, login, lançamentos, parcelamento, acerto, listagem e dashboard. Achado: **nenhum teste cobre o endpoint `/api/export`** — nem autenticação do endpoint, nem o conteúdo do arquivo gerado.

## Pendente
- Nenhum arquivo do sprint ficou sem revisão.

## Veredito parcial
APROVADO COM RESSALVAS — sem item bloqueante de segurança. Dois itens recomendados e um opcional.
