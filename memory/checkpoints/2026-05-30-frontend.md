# Checkpoint Frontend — 2026-05-30

## Feito nesta sessão

### Sprint 06 — S-06-02 Tela de Recorrências

Branch: `feature/s06-frontend-recurring` (4 commits, push realizado)

**Commits:**
1. `77a7401` — modelos (`RecurringRuleResponse`, `CreateRecurringRuleRequest`) e `RecurringRuleService` (getAll, create, deactivate) e `recurring.routes.ts`
2. `0703e31` — `RecurringFormComponent` (dialog MatDialog com Reactive Form completo)
3. `cb7b09a` — `RecurringListComponent` (tabela desktop + cards mobile + empty state)
4. `d2d67b3` — rota `/recurring` em `app.routes.ts` + atalho no `DashboardComponent`

**Build:** `npm run build` — BUILD SUCCESS, 0 erros, 0 warnings.

### Estrutura criada (recorrências)

```
frontend/src/app/recurring/
  models/recurring-rule.models.ts
  recurring-rule.service.ts
  recurring.routes.ts
  recurring-form/
    recurring-form.component.ts   ← dialog MatDialog
    recurring-form.component.html
    recurring-form.component.scss
  recurring-list/
    recurring-list.component.ts
    recurring-list.component.html
    recurring-list.component.scss
```

### Funcionalidades implementadas (recorrências)

- Listagem de todas as regras ativas via `GET /api/recurring-rules`
- Tabela Angular Material para desktop (8 colunas)
- Cards responsivos para mobile
- Chip de tipo com cor coral (EXPENSE) / verde-menta (INCOME)
- Frequência em pt-br: MONTHLY=Mensal, WEEKLY=Semanal, YEARLY=Anual
- Dialog de criação com todos os campos requeridos pelo contrato da API
- Validações Reactive Form (required, min 0.01)
- Desativação com confirm() nativo + feedback via MatSnackBar
- Empty state com CTA
- FAB mobile / botão desktop para nova regra
- Interceptor JWT já configurado globalmente (sem HttpClientModule)
- Atalho "Recorrências" adicionado ao DashboardComponent

---

### S-06-04 Tela de Acerto de Contas — COMPLETO

Branch: `feature/s06-frontend-settlement` (3 commits, push realizado)

Arquivos criados:

1. `frontend/src/app/settlement/settlement.model.ts`
   - Interface `PersonSettlementDTO` (id, name, totalPaid, shouldPay, balance)
   - Interface `SettlementResponse` (month, totalExpense, personA, personB, debtor, creditor, amountOwed, settled, pendingProportional, pendingMessage)

2. `frontend/src/app/settlement/settlement.service.ts`
   - `SettlementService` com método `getSettlement(month?: string)`
   - Usa `HttpParams` para passar `month` em formato `yyyy-MM`
   - Se `month` não informado, usa mês atual automaticamente

3. `frontend/src/app/settlement/settlement.routes.ts`
   - Rota lazy-load para `SettlementComponent`

4. `frontend/src/app/settlement/settlement.component.ts` (standalone, OnPush)
   - Sinais: `loading`, `data`, `currentDate`
   - Computeds: `monthLabel`, `currentMonth`, `creditorPerson`, `debtorPerson`
   - Navegação por mês (anterior/próximo)
   - `formatBalance()` com sinal explícito +/−
   - `formatCurrency()` em pt-BR

5. `frontend/src/app/settlement/settlement.component.html`
   - Header com logo, toggle de tema, botão home
   - Navegação por mês
   - Card principal com: total do mês, chip "Acertado" (verde), alerta proporcional (coral), destaque do débito (fonte Fraunces grande)
   - Cards lado a lado por pessoa: Pagou / Deveria pagar / Saldo (±)
   - Legenda informativa
   - Estado vazio com link para lançamentos

6. `frontend/src/app/settlement/settlement.component.scss`
   - Design System completo: CSS variables, Fraunces para valores, Plus Jakarta Sans para corpo
   - Responsivo: grid 1 coluna → 2 colunas no breakpoint 560px
   - Saldo positivo = var(--color-income) verde, negativo = var(--color-expense) coral
   - Alerta proporcional com borda coral
   - Destaque de débito com fundo areia

### Arquivos modificados (settlement)

- `frontend/src/app/app.routes.ts`: adicionada rota `/settlement` com `authGuard`
- `frontend/src/app/dashboard/dashboard.component.ts`: atalho "Acerto" habilitado com `routerLink="/settlement"`

**Build:** `npm run build` — BUILD SUCCESS, 0 erros, 0 warnings.
Chunk gerado: `settlement-component` 15.29 kB (3.86 kB gzip).

## Pendente

- Integração com backend real em ambiente de desenvolvimento (testar ponta a ponta após `docker compose up --build`)
- Possível ajuste de nomes de Person A/B no `splitRuleLabel()` caso a API retorne os nomes reais

## Proximo passo imediato

DevOps: merge de todas as branches do sprint-06 para master e criacao da tag `sprint-06`.
