# Checkpoint Banco — 2026-05-29 (Sprint 06)

## Feito nesta sessão

### Auditoria recurring_rule (V2) — TUDO OK, nenhuma alteração necessária
- Colunas: id, type, amount, description, category_id, paid_by_person_id, payment_method, split_rule, frequency, next_date, active, created_at, version — TODAS PRESENTES
- Índice parcial: idx_recurring_rule_next_date_active ON recurring_rule(next_date) WHERE active = true — PRESENTE
- FKs: category_id → category, paid_by_person_id → person — PRESENTES
- Constraints CHECK: type IN ('EXPENSE','INCOME'), amount > 0, payment_method IN (...), split_rule IN (...), frequency IN ('MONTHLY','WEEKLY','YEARLY') — TODAS PRESENTES

### Migration V9__sprint06_settlement_indexes.sql — CRIADA E COMMITADA
- Branch: feature/s06-schema
- Commit: 00a0b28
- Conteúdo: CREATE INDEX idx_transaction_type_date ON transaction (type, date)
- Motivação: SettlementService faz query combinando type='INCOME' + date BETWEEN.
  Índice composto evita Bitmap AND entre idx_transaction_type (V8) e idx_transaction_date (V2).
  Os índices individuais são mantidos para queries com filtro único.
- Status: push feito para origin/feature/s06-schema

## Estado atual dos índices em transaction
| Índice | Colunas | Migration |
|---|---|---|
| idx_transaction_date | (date) | V2 |
| idx_transaction_category_id | (category_id) | V2 |
| idx_transaction_paid_by_person_id | (paid_by_person_id) | V2 |
| idx_transaction_card_id | (card_id) | V6 |
| idx_transaction_type | (type) | V8 |
| idx_transaction_type_date | (type, date) | V9 ← NOVO |

## Pendente
- Nenhuma migration de schema pendente para Sprint 06
- recurring_rule já tem tudo que o RecurringRuleService/Job precisam
- SettlementService agora tem suporte de índice adequado

## Próximo passo imediato
Abrir PR de feature/s06-schema → master para revisão.
Se o agente Backend precisar de novas colunas (ex: campo de idempotência no job
de recorrência), criar V10 nesta mesma branch.
