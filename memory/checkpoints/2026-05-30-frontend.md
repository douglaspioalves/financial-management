# Checkpoint Frontend — 2026-05-30 02:15

## Feito nesta sessão

### Branch: `feature/s07-frontend-ux`

Commits realizados (todos nesta branch):

1. `4cdfe77` — `feat(export): botão de exportar CSV/Excel na tela de lançamentos`
   - Criado `ExportService` (`/frontend/src/app/core/services/export.service.ts`)
   - Adicionado `MatMenuModule` no `TransactionListComponent`
   - Botão de download (ícone `download`) no header da tela de lançamentos
   - Menu com "Exportar CSV" e "Exportar Excel"
   - Chama `GET /api/export?month=YYYY-MM&format=csv|xlsx` e faz download do blob
   - Signal `exporting` para estado de loading durante exportação

2. `ecdd66d` — `feat(ux): skeleton de carregamento nos cards de resumo do dashboard`
   - Dashboard: cards de resumo (Receitas, Despesas, Saldo) exibem skeleton animado durante loading
   - CSS animation `skeleton-pulse` com opacidade pulsante

3. `9e85202` — `feat(ux): loading states e responsividade em acerto de contas e recorrências`
   - Settlement: skeleton de carregamento com cards fantasma antes dos dados
   - Settlement: media query 375px para fontes e padding menores
   - Recurring: spinner centralizado na área de conteúdo
   - `MatProgressSpinnerModule` adicionado ao `RecurringListComponent`
   - Recurring: media query 375px

4. `0dc2fbe` — `feat(ux): responsividade mobile — breakpoints 375px em todos os componentes`
   - Global styles: ajuste de dialog e snackbar em 375px
   - `installments-table-wrapper` com `overflow-x: auto` para scroll horizontal em mobile

### Estado do build
`npm run build` → `Application bundle generation complete` — **sem erros**

## Pendente

- A tela TransactionList já tinha loading state e empty state; foi melhorada
- Dashboard já tinha empty states nas seções de gráfico e lista; foi melhorada com skeleton
- Cards list já tinha loading state e empty state — não alterada (já estava completa)
- Budget list já tinha skeleton loading e empty state — não alterada (já estava completa)
- Não há interceptor global de erros — cada serviço trata via subscribe error handler com snackbar

## Próximo passo imediato

Abrir PR de `feature/s07-frontend-ux` → `master` para revisão do agente revisor.
