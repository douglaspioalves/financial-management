# Checkpoint Banco — 2026-05-28 19:57

## Feito nesta sessão

- **V6__add_transaction_card_index.sql** criada em `backend/src/main/resources/db/migration/`
  - Cria `CREATE INDEX idx_transaction_card_id ON transaction (card_id);`
  - Motivação: CardService fará `SELECT ... WHERE card_id = ?` para bloquear exclusão de cartão com transações; sem o índice seria full scan.
  - Validação: `./mvnw test` → BUILD SUCCESS (38 testes passando, 0 falhas)
- Branch `feature/s03-schema` criada a partir de `master`
- Commit pendente de permissão Bash

## Schema atual (migrations aplicadas)

| Migration | Conteúdo |
|---|---|
| V1__baseline.sql | Marcador baseline Flyway |
| V2__initial_schema.sql | Schema completo (users, person, category, card, transaction, installment, budget, recurring_rule) + índices iniciais |
| V3__seeds.sql | Seeds: 2 persons + categorias iniciais |
| V4__add_category_inactive.sql | Coluna `inactive` em category |
| V5__seed_default_users.sql | Seeds dos dois usuários padrão |
| V6__add_transaction_card_index.sql | Índice `idx_transaction_card_id` em `transaction(card_id)` |

## Índices existentes após V6

- `idx_transaction_date` — transaction.date
- `idx_transaction_category_id` — transaction.category_id
- `idx_transaction_paid_by_person_id` — transaction.paid_by_person_id
- `idx_transaction_card_id` — transaction.card_id (NOVO — V6)
- `idx_installment_reference_month` — installment.reference_month
- `idx_installment_transaction_id` — installment.transaction_id
- `idx_budget_month` — budget.month
- `idx_recurring_rule_next_date_active` — recurring_rule.next_date WHERE active = true

## Pendente

- Commit e push da branch `feature/s03-schema` (aguarda permissão Bash)
- Atualizar status S-03-00 em sprint-03.md para CONCLUIDO

## Proximo passo imediato

Aguardar merge de `feature/s03-schema` via agente revisor/DevOps. Backend pode iniciar S-03-01 (entidade Card + CardRepository) usando a branch `feature/s03-backend`.
