# Daily — 2026-05-26
**Sprint:** 01 | **Dia do sprint:** 1/10
**Agentes ativos:** DevOps

---

## MANHA — Planejamento do dia

### Backend
- **O que vou fazer hoje:** aguardando S-01-01 (esqueleto) para iniciar.
- **Dependências/bloqueios antecipados:** depende do projeto Spring Boot ser gerado pelo DevOps.

### Frontend
- **O que vou fazer hoje:** aguardando S-01-01 (esqueleto) para iniciar.
- **Dependências/bloqueios antecipados:** depende do projeto Angular ser gerado pelo DevOps.

### DBA
- **O que vou fazer hoje:** aguardando S-01-01 para preparar migrations.
- **Dependências/bloqueios antecipados:** S-01-02 só começa após S-01-01 concluída.

### DevOps
- **O que vou fazer hoje:**
  - [ ] Criar projeto Spring Boot via Spring Initializr (Web, JPA, Security, Validation, Flyway, PostgreSQL)
  - [ ] Criar projeto Angular + Angular Material
  - [ ] Escrever docker-compose.yml (postgres, backend, frontend)
  - [ ] Criar .env.example com variáveis de ambiente
  - [ ] Iniciar README com instruções de instalação no Windows (PowerShell)
- **Dependências/bloqueios antecipados:** nenhum — S-01-01 é o ponto de partida do sprint.

---

## FIM DO DIA — Fechamento

### Backend
- **O que foi feito:** —
- **O que ficou para amanhã:** —
- **Impedimentos:** —
- **Status das stories:** aguardando S-01-01

### Frontend
- **O que foi feito:** —
- **O que ficou para amanhã:** —
- **Impedimentos:** —
- **Status das stories:** aguardando S-01-01

### DBA
- **O que foi feito:** —
- **O que ficou para amanhã:** —
- **Impedimentos:** —

### DevOps
- **O que foi feito:**
  - Criado projeto Spring Boot 3.4.5 com Java 21 (Web, Data JPA, Security, Validation, Flyway, PostgreSQL, Lombok, H2-test)
  - Criado projeto Angular 21 com Angular Material, SCSS, routing
  - Implementado `GET /api/health` retornando `{ "status": "ok" }`
  - Configurado Spring Security (CSRF desabilitado, sessao stateless, /api/health e /api/auth/** publicos, TODO JWT)
  - Criado componente AppComponent com chamada ao health check e exibicao de status na tela
  - Criado HealthService Angular que consome GET /api/health
  - Criado docker-compose.yml com 3 servicos (postgres:16-alpine, backend, frontend)
  - Criado .env.example com variaveis de ambiente documentadas
  - Criado .gitignore cobrindo .env, target/, node_modules/, .angular/, dist/, .idea/, .vscode/, *.iml
  - Criado Dockerfile multi-stage para backend (eclipse-temurin:21) e frontend (node:20 + nginx:alpine)
  - Criado nginx.conf com proxy reverso /api/ → backend e suporte a SPA (Angular Router)
  - Atualizado README.md com instrucoes completas em pt-br para Windows (PowerShell)
  - Migration baseline V1__baseline.sql criada (vazia — schema completo vira na S-01-02)
  - Maven wrapper gerado (mvnw + mvnw.cmd + .mvn/wrapper/maven-wrapper.properties)
  - Build do backend validado: `mvnw package -DskipTests` — JAR gerado com sucesso
  - Build do frontend validado: `ng build --configuration production` — bundle gerado em dist/frontend/browser
- **O que ficou para amanhã:** aguardar S-01-02 (DBA) para migrations de schema
- **Impedimentos:** nenhum

---

## Impedimentos do dia

| Impedimento | Agente | Impacto | Acao |
|---|---|---|---|
| — | — | — | — |

## Progresso do sprint (atualizado)

- Stories concluidas hoje: S-01-01 (DevOps — esqueleto + Docker Compose) — CONCLUIDA
- Stories em andamento: nenhuma
- Stories desbloqueadas: S-01-02 (DBA pode iniciar migrations)
- Stories bloqueadas: S-01-03, S-01-04 (dependem de S-01-02)
