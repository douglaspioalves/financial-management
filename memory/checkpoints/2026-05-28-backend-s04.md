# Checkpoint Backend — 2026-05-28 20:50

## Feito nesta sessão (S-04-01)

### Arquivos criados
- `backend/src/main/java/com/gastos/domain/Installment.java` — entidade JPA mapeando tabela `installment` (sem @Version, pois o schema não tem coluna version)
- `backend/src/main/java/com/gastos/repository/InstallmentRepository.java` — JpaRepository com `findByTransactionIdOrderByNumberAsc`
- `backend/src/main/java/com/gastos/dto/installment/InstallmentResponse.java` — record DTO: id, number, amount, referenceMonth
- `backend/src/main/java/com/gastos/service/InstallmentService.java` — geração de parcelas com algoritmo canônico de reference_month e arredondamento DOWN

### Arquivos modificados
- `backend/src/main/java/com/gastos/dto/transaction/TransactionResponse.java` — substituído campo `UUID cardId` por `CardSummary card` (com id e name); null para não-CREDIT
- `backend/src/main/java/com/gastos/service/TransactionService.java` — remove placeholder, integra parcelamento, ajusta toResponse(), adiciona findInstallments(), novas validações
- `backend/src/main/java/com/gastos/controller/TransactionController.java` — novo endpoint `GET /{id}/installments`

### Branch e commits
- Branch: `feature/s04-backend` (push feito para origin)
- Commits:
  1. `36aef98` — entidade Installment + InstallmentRepository + InstallmentResponse DTO
  2. `3ad8340` — InstallmentService.generateInstallments()
  3. `74bab5b` — TransactionService: remove placeholder, integra parcelamento, ajusta toResponse()
  4. `5253b87` — TransactionController: endpoint GET /{id}/installments

### Testes
- `./mvnw test` → BUILD SUCCESS: 58 testes passando (sem falhas)
- InstallmentServiceTest (12 casos de borda) está na branch `feature/s04-tests` criada pelo agente QA

## Pendente

- PR da `feature/s04-backend` para `master` aguarda revisão do agente revisor
- Frontend (S-04-02): badge "X/N" e expansão de parcelas — branch `feature/s04-frontend`
- Merge da `feature/s04-tests` para `master` também aguarda revisão

## Próximo passo imediato

Agente Revisor: revisar `feature/s04-backend` (lógica de parcelamento, TransactionResponse com CardSummary, validações de installmentsTotal).

Agente Frontend: implementar S-04-02 — badge e detalhe de parcelas, consumindo:
- `GET /api/transactions/{id}/installments` → `InstallmentResponse[]`
- Campo `card` no `TransactionResponse` (objeto com id+name, não mais `cardId`)
