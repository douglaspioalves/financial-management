# Checkpoint DevOps — 2026-05-29 20:13

## Feito nesta sessão

### Merges realizados para master (Sprint 05)
- `docs/s05-planning` → merge OK (0f5971e): docs/api.md, planning-sprint-05.md
- `docs/s05-review` → merge OK (bebb5a0): review-sprint-05.md
- `feature/s05-schema` → merge OK (294bb74): V8__sprint05_dashboard_indexes.sql
- `feature/s05-backend-dashboard` → merge OK (91a9cf1): DashboardService, DashboardController, DTOs
- `feature/s05-backend-budget` → merge OK com conflito resolvido (b72b821):
  - Conflito em `InstallmentRepository.java` — combinados `findExpenseInstallmentsByMonth` (dashboard) e `sumByCategoryAndReferenceMonth` (budget)
  - Conflito em `TransactionRepository.java` — combinados `findCashExpensesByMonth`, `findIncomesByMonth`, `findRecentByMonth` (dashboard) e `sumNonInstallmentByTypeAndCategoryAndDateBetween` (budget)
- `fix/budget-remaining-amount` → merge OK (a0969dc): `remainingAmount` no BudgetResponse
- `feature/s05-tests` → merge OK com conflito resolvido (e6203ce):
  - Conflito em `BudgetServiceTest.java` — mantida versão completa do HEAD (mocks reais) sobre a versão @Disabled do branch de testes
  - Novos arquivos: `BudgetIntegrationTest.java`, `DashboardIntegrationTest.java`
- `fix/sprint05-tests` → merge OK (83cd4f6): correções TC-D6/D8 e javadoc WARNING
- `feature/s05-frontend-dashboard` → merge OK (2f843c6): DashboardComponent com ng2-charts, dashboard.routes.ts
- `feature/s05-frontend-budget` → merge OK com conflito resolvido (7999a84):
  - Conflito R-01 em `app.routes.ts` — todas as rotas mantidas (transactions, cards, dashboard, budget); wildcard → `/dashboard`

### Validações
- `mvn test` → BUILD SUCCESS — 102 testes, 0 falhas, 17 skipped (integração sem banco)
- `npm run build` → Build sem erros, bundle gerado em `frontend/dist/frontend/`
- `git push origin master` → OK (f1841e6..7999a84)
- `git tag -a sprint-05` → tag criada apontando para 7999a84

### Arquivos atualizados
- `memory/sprints/sprint-05.md` — status → ✅ CONCLUÍDO, todas as tasks → CONCLUÍDO

## Pendente

- `docker compose up --build` com banco zerado (validação end-to-end completa)
  - Não executado nesta sessão por limitação de ambiente Docker
- Testes de integração `BudgetIntegrationTest` e `DashboardIntegrationTest` requerem banco real (17 skipped)

## Próximo passo imediato

Iniciar Sprint 06 — Acerto de Contas (quem deve quanto a quem, com parcelas).
O Agente Arquiteto deve detalhar o contrato de API do acerto de contas antes de qualquer implementação.
