# Checkpoint Revisor — 2026-05-29 (Sprint 04)

## Revisado nesta sessão

### feature/s04-backend
- `Installment.java` — entidade JPA; campos corretos, BigDecimal, sem @Version própria (adequado). ✓
- `InstallmentService.java` — algoritmo de reference_month e arredondamento. Casos normais corretos. Dois itens de borda identificados (R-01, R-02). ⚠
- `InstallmentRepository.java` — método findByTransactionIdOrderByNumberAsc. ✓
- `InstallmentResponse.java` — DTO record, BigDecimal, sem vazamento. ✓
- `TransactionController.java` — endpoint GET /{id}/installments adicionado. Sem anotação de segurança explícita, mas coberto pelo SecurityConfig global. ✓
- `TransactionService.java` — integração com InstallmentService, findInstallments(), N+1 identificado (O-02). ✓
- `TransactionResponse.java` — CardSummary adicionado com id/name. ✓
- `GlobalExceptionHandler.java` — sem handler para MethodArgumentTypeMismatchException; Spring Boot default produz 400. ✓
- `SecurityConfig.java` — anyRequest().authenticated() cobre o novo endpoint. ✓

### feature/s04-schema
- `V7__sprint04_noop.sql` — auditoria de schema, SELECT 1, sem DDL. Correto. ✓

### feature/s04-tests
- `InstallmentServiceTest.java` — 12 testes unitários. Casos TC-01 a TC-EXTRA-02 cobertos. Ausência de teste de integração para o endpoint (R-03). ⚠

### feature/s04-frontend
- `installment.service.ts` — HTTP GET correto, usa baseUrl /api/transactions. ✓
- `transaction.models.ts` — CardSummary com closingDay/dueDay não retornados pelo backend (O-01). ⚠
- `transaction-list.component.html` — badge X/N só para parcelados, expansion panel com lazy load. ✓
- `transaction-list.component.ts` — lazy loading correto, snackbar pt-br, estimativa badge (O-03). ✓
- `transaction-list.component.scss` — cores lilás (#b8a9d9) e verde-menta (#7fc4a0) do design system, dark mode duplo (media query + host-context). ✓

## Pendente

Nada pendente — revisão completa de todos os arquivos das 4 branches.

## Veredito parcial

**APROVADO COM RESSALVAS**

Nenhum item **BLOQUEANTE** encontrado. A fatia pode ser mergeada após endereçar os itens recomendados:

- **R-01** (RECOMENDADO): Harmonizar CLAUDE.md vs código para o limite exato do dia de fechamento (`<=` vs `<`)
- **R-02** (RECOMENDADO): Corrigir algoritmo para `closingDay > dias do mês` usando `Math.min(closingDay, lengthOfMonth())`
- **R-03** (RECOMENDADO): Adicionar teste de integração para o endpoint `/api/transactions/{id}/installments` (401, 200, 404)

Itens opcionais (O-01, O-02, O-03) podem ir para o backlog.
