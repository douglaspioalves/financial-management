# Checkpoint Revisor — 2026-05-29 (Sprint 05)

## Revisado nesta sessão

### feature/s05-schema
- `V8__sprint05_dashboard_indexes.sql` — apenas `CREATE INDEX idx_transaction_type ON transaction (type)`. Auditoria de índices existentes bem documentada. Sem DDL destrutivo. ✓

### feature/s05-backend-dashboard
- `DashboardController.java` — endpoint GET, `required=false`, fallback para `YearMonth.now()`. JWT coberto pelo SecurityConfig global. ✓
- `DashboardService.java` — agregação correta (cash por date, parcelas por referenceMonth), sem dupla contagem, variação null quando mês anterior = 0, limite de 10 em recentTransactions. ✓
- `DashboardResponse.java`, `MonthSummaryDTO.java`, `CategoryExpenseDTO.java`, `RecentTransactionDTO.java` — BigDecimal, LocalDate, sem campos sensíveis. ✓
- `TransactionRepository.java` (dashboard) — `findCashExpensesByMonth`, `findIncomesByMonth`, `findRecentByMonth` com JOIN FETCH. ✓
- `InstallmentRepository.java` (dashboard) — `findExpenseInstallmentsByMonth` com JOIN FETCH. ✓

### feature/s05-backend-budget
- `Budget.java` — entidade com `@Version`, `BigDecimal`, `LocalDate`, mapeamento `"month"` com aspas para H2. ✓
- `BudgetService.java` — CRUD completo, optimistic locking manual, validateFirstDayOfMonth, calculateSpent com mesma regra do dashboard. REPROVADO: `remainingAmount` ausente no `BudgetResponse`. ✗ (B-01)
- `BudgetController.java` — validação com `@Valid`, rotas corretas, 201/200/204. ✓
- `BudgetRequest.java` — `@NotNull`, `@Positive`, mensagens pt-br. ✓
- `BudgetResponse.java` — falta campo `remainingAmount`. ✗ (B-01)
- `BudgetRepository.java` — métodos necessários presentes. ✓
- `TransactionRepository.java` (budget) — `sumNonInstallmentByTypeAndCategoryAndDateBetween` com COALESCE. ✓
- `InstallmentRepository.java` (budget) — `sumByCategoryAndReferenceMonth` com COALESCE. ✓

### feature/s05-backend-budget (testes unitários)
- `BudgetServiceTest.java` — 14 testes; cobre validate, CRUD, status OK/WARNING/EXCEEDED, optimistic locking, calculateSpent. ✓

### feature/s05-tests
- `DashboardIntegrationTest.java` — `@Disabled`. TC-D1 a TC-D8. Erros a corrigir antes de habilitar: TC-D6 (400 vs 200 na ausência de `month`) e TC-D8 (`$.byCategory` vs `$.expenseByCategory`). ⚠ (R-02, R-03)
- `BudgetIntegrationTest.java` — `@Disabled`. TC-B1 a TC-B9. Javadoc com limiar errado (75% vs 70%). Testes em si corretos. ⚠ (R-04)

### feature/s05-frontend-dashboard
- `DashboardComponent.ts` — signals, ChangeDetectionStrategy.OnPush, snackbar, formatCurrency/Date pt-br, gráfico donut reativo. ✓
- `dashboard.service.ts` — HTTP GET com params. ✓
- `dashboard.models.ts` — interfaces alinhadas com DashboardResponse backend. ✓
- `dashboard.routes.ts` — `loadComponent`. ✓
- `app.routes.ts` (dashboard branch) — redirect `/` → `/dashboard`, `/budget` com authGuard, usa `loadChildren`. Conflito com branch budget ⚠ (R-01)
- `app.config.ts` — `provideCharts(withDefaultRegisterables())`. ✓

### feature/s05-frontend-budget
- `budget.service.ts` — CRUD HTTP completo, versão no update. ✓
- `budget.model.ts` — declara `remainingAmount: number` que backend não retorna. ✗ (B-01)
- `budget.routes.ts` — lazy loading. ✓
- `BudgetListComponent.ts` — signals, snackbar pt-br, statusColor/statusClass corretos, progressValue (capped 100%). ✓
- `BudgetListComponent.html` — usa `budget.remainingAmount` diretamente. ✗ (B-01)
- `budget-form.component.ts` — FormBuilder, editando categoria desabilitada, version no update. ✓
- `app.routes.ts` (budget branch) — redirect `/` → `/auth/login` (diverge da branch dashboard), sem rota `/budget` no arquivo. ✗ (R-01)

## Pendente

Nada pendente — todas as branches foram revisadas integralmente.

## Veredito parcial

**REPROVADO**

Item bloqueante: **B-01** — `remainingAmount` calculado e exibido no frontend mas ausente na resposta do backend. Causa exibição de NaN para todos os orçamentos.

Itens recomendados a corrigir antes do merge: R-01, R-02, R-03, R-04.
